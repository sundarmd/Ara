-r requirements.txt

# Optional test runner stack for local development and CI.
# The current suite is compatible with stdlib unittest:
#   python -m unittest discover -s tests -v
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
